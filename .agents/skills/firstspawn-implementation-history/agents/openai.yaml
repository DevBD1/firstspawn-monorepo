interface:
  display_name: "FirstSpawn Implementation History"
  short_description: "Record concise continuation notes"
  default_prompt: "Use $firstspawn-implementation-history to document meaningful FirstSpawn implementation work."
policy:
  allow_implicit_invocation: true
