# --- 1. GLOBAL HTTPS REDIRECT ---
# This ensures EVERYTHING (api, db, logs, webmail) goes to SSL.
server {
    listen 80;
    listen [::]:80;
    server_name .firstspawn.com;
    return 301 https://$host$request_uri;
}

# --- 2. API (FastAPI) ---
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name api.firstspawn.com;

    ssl_certificate /etc/nginx/certs/firstspawn.crt;
    ssl_certificate_key /etc/nginx/certs/firstspawn.key;

    location / {
        auth_basic "Auth needed!";
        auth_basic_user_file /etc/nginx/.htpasswd;

        proxy_pass http://fastapi:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# --- 3. DB (pgAdmin) ---
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name db.firstspawn.com;

    ssl_certificate /etc/nginx/certs/firstspawn.crt;
    ssl_certificate_key /etc/nginx/certs/firstspawn.key;

    location / {
        proxy_pass http://pgadmin:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_redirect off;
    }
}

# --- 4. LOGS (Dozzle) ---
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name logs.firstspawn.com;

    ssl_certificate /etc/nginx/certs/firstspawn.crt;
    ssl_certificate_key /etc/nginx/certs/firstspawn.key;

    location / {
        auth_basic "Auth needed!";
        auth_basic_user_file /etc/nginx/.htpasswd;

        proxy_pass http://dozzle:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# --- 5. WEBMAIL (Roundcube/Snappy) ---
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name webmail.firstspawn.com;

#     ssl_certificate /etc/nginx/certs/firstspawn.crt;
#     ssl_certificate_key /etc/nginx/certs/firstspawn.key;

#     location / {
#         proxy_pass http://webmail:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
        
#         # Roundcube büyük ekler için gerekebilir
#         client_max_body_size 64M;
#     }
# }